global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'scm-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Pointing to our Java app running in IntelliJ environment.